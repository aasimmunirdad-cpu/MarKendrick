# Core runtime dependencies for server.py — trimmed from Emergent's original
# template, which included unused packages (Stripe, OpenAI, Google GenAI SDKs,
# boto3/s3, litellm from an Emergent-hosted wheel URL). None of those are
# imported anywhere in server.py, so they were removed for a clean, portable
# install on any host (Railway, Render, a VPS, etc).

fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
motor==3.6.0
pymongo==4.9.2
pydantic==2.13.4
email-validator==2.3.0
bcrypt==4.1.3
PyJWT==2.13.0
httpx==0.28.1
python-dotenv==1.2.2
python-multipart==0.0.32

# Used by scripts/gen_whitepapers.py to (re)generate the branded PDF reports.
# Not required for the API server itself, kept for convenience.
reportlab==5.0.0

# NOTE: scripts/gen_team_photos.py depends on Emergent's `emergentintegrations`
# package (Gemini image generation via Emergent's proxy) and is NOT included
# here — it only runs inside Emergent's own environment. The 4 generated
# headshots are already saved as static files in frontend/public/media/team/,
# so this script does not need to run again unless you want new photos
# through Emergent specifically.
